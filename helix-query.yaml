indexes:
  query-index:
    source: /query-index.xlsx
    sheet: helix-default
    columns:
      - path
      - title
      - description
      - image
      - lastModified
    # Optional: filter out items with noindex
    filter:
      robots: "!noindex"

